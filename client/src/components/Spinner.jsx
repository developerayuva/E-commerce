import React from "react";
import spinner from "../images/Spinner.gif"

function Spinner() {
    return (
        <img src={spinner} alt="spinner" className="d-block mx-auto"/>
    )
}

export default Spinner;